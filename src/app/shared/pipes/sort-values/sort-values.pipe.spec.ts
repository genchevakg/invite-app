import { SortValuesPipe } from './sort-values.pipe';

describe('SortValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
