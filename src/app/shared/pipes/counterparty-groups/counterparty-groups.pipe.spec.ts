import { CounterpartyGroupsPipe } from './counterparty-groups.pipe';

describe('CounterpartyGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new CounterpartyGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
